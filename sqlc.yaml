version: "2"

sql:
  - engine: "postgresql"
    queries: "postgres/queries"
    schema: "postgres/migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"